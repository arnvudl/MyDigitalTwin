# Local development dependencies only.
#
# Docker is the source of truth for runtime dependencies:
# - Dockerfile installs Spark, Delta, notebooks and ML tooling.
# - Dockerfile.app installs the Dash dashboard runtime.
#
# Use this file only when you want a lightweight local environment for tests,
# notebooks, schema checks or Copier onboarding.

jupyter
ipython
pytest
pandera
copier
