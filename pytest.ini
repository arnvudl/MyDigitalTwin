[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers to categorize tests
markers =
    unit: Fast unit tests that don't require the full dataset.
    data_quality: Slower tests that validate the contents of the data warehouse.

# Default command line options
addopts = -v --strict-markers
